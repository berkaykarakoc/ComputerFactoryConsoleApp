package business;

public class RegularPC extends PC {
	
	public RegularPC(CPU cpu, RAM ram, Monitor monitor, Headphone headphone, Keyboard keyboard, Mouse mouse) {
		super(cpu, ram, monitor, headphone, keyboard, mouse);
	}

}
